import 'package:flutter/material.dart';

class DermatologistsPage extends StatelessWidget {
  const DermatologistsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dermatologists Page'),
    );
  }
}